version: '3.8'

services:
  consumer:
    build:
      context: .
      dockerfile: WebApplicationConsumer/Dockerfile
    ports:
      - "8082:80"
    restart: always

  producer:
    build:
      context: .
      dockerfile: WebApplicationProducer/Dockerfile
    ports:
      - "8083:80"
    restart: always
